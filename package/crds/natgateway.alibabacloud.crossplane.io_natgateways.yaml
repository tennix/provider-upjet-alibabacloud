---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: natgateways.natgateway.alibabacloud.crossplane.io
spec:
  group: natgateway.alibabacloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: NATGateway
    listKind: NATGatewayList
    plural: natgateways
    singular: natgateway
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NATGateway is the Schema for the NATGateways API. Provides a
          resource to create a VPC NAT Gateway.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NATGatewaySpec defines the desired state of NATGateway
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accessMode:
                    description: The access mode for reverse access to the VPC NAT
                      gateway. See access_mode below.
                    items:
                      properties:
                        modeValue:
                          description: 'The mode of Access. Valid values:'
                          type: string
                        tunnelType:
                          description: 'The type of Tunnel. Valid values: geneve.
                            NOTE: tunnel_type takes effect only if mode_value is set
                            to tunnel.'
                          type: string
                      type: object
                    type: array
                  bandwidthPackages:
                    description: (Removed since v1.121.0) A list of bandwidth packages
                      for the nat gatway. See bandwidth_packages below.
                    items:
                      properties:
                        bandwidth:
                          description: '(Removed since v1.121.0) The bandwidth value
                            of the current bandwidth package. NOTE: Field bandwidth
                            has been removed from provider version 1.121.0.'
                          type: number
                        ipCount:
                          description: '(Removed since v1.121.0) The IP number of
                            the current bandwidth package. NOTE: Field ip_count has
                            been removed from provider version 1.121.0.'
                          type: number
                        zone:
                          description: '(Removed since v1.121.0) The AZ for the current
                            bandwidth. NOTE: Field zone has been removed from provider
                            version 1.121.0.'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: 'Whether enable the deletion protection or not. Default
                      value: false.'
                    type: boolean
                  description:
                    description: Description of the nat gateway, This description
                      can have a string of 2 to 256 characters, It cannot begin with
                      http:// or https://. Defaults to null.
                    type: string
                  dryRun:
                    description: 'Specifies whether to only precheck this request.
                      Default value: false.'
                    type: boolean
                  eipBindMode:
                    description: 'The EIP binding mode of the NAT gateway. Default
                      value: MULTI_BINDED. Valid values:'
                    type: string
                  force:
                    description: Specifies whether to forcefully delete the NAT gateway.
                    type: boolean
                  icmpReplyEnabled:
                    description: 'Specifies whether to enable ICMP retrieval. Default
                      value: true. Valid values:'
                    type: boolean
                  internetChargeType:
                    description: 'The internet charge type. Valid values PayByLcu.
                      The PayByLcu is only support enhanced NAT. NOTE: From version
                      1.137.0, internet_charge_type cannot be set to PayBySpec.'
                    type: string
                  natGatewayName:
                    description: Name of the nat gateway. The value can have a string
                      of 2 to 128 characters, must contain only alphanumeric characters
                      or hyphens, such as "-",".","_", and must not begin or end with
                      a hyphen, and must not begin with http:// or https://. Defaults
                      to null.
                    type: string
                  natType:
                    description: to Enhanced and set vswitch_id.
                    type: string
                  networkType:
                    description: 'Indicates the type of the created NAT gateway. Valid
                      values internet and intranet. internet: Internet NAT Gateway.
                      intranet: VPC NAT Gateway.'
                    type: string
                  paymentType:
                    description: The billing method of the NAT gateway. Valid values
                      are PayAsYouGo. Default to PayAsYouGo.
                    type: string
                  period:
                    description: |-
                      The duration that you will buy the resource, in month. It is valid when payment_type is Subscription. Valid values: [1-9, 12, 24, 36]. At present, the provider does not support modify "period" and you can do that via web console. NOTE: International station only supports Subscription.
                      -> NOTE: The attribute period is only used to create Subscription instance or modify the PayAsYouGo instance to Subscription.
                    type: number
                  privateLinkEnabled:
                    description: 'Specifies whether to enable PrivateLink. Default
                      value: false. Valid values:'
                    type: boolean
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  spec:
                    description: '(Removed since v1.121.0) The specification of the
                      nat gateway. NOTE: Field spec has been deprecated from provider
                      version 1.7.1, and it has been removed from provider version
                      1.121.0. New field specification instead.'
                    type: string
                  specification:
                    description: The specification of the nat gateway. Valid values
                      are Small, Middle and Large. Effective when internet_charge_type
                      is PayBySpec and network_type is internet. Details refer to
                      Nat Gateway Specification.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The VPC ID.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchId:
                    description: The id of VSwitch.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessMode:
                    description: The access mode for reverse access to the VPC NAT
                      gateway. See access_mode below.
                    items:
                      properties:
                        modeValue:
                          description: 'The mode of Access. Valid values:'
                          type: string
                        tunnelType:
                          description: 'The type of Tunnel. Valid values: geneve.
                            NOTE: tunnel_type takes effect only if mode_value is set
                            to tunnel.'
                          type: string
                      type: object
                    type: array
                  bandwidthPackages:
                    description: (Removed since v1.121.0) A list of bandwidth packages
                      for the nat gatway. See bandwidth_packages below.
                    items:
                      properties:
                        bandwidth:
                          description: '(Removed since v1.121.0) The bandwidth value
                            of the current bandwidth package. NOTE: Field bandwidth
                            has been removed from provider version 1.121.0.'
                          type: number
                        ipCount:
                          description: '(Removed since v1.121.0) The IP number of
                            the current bandwidth package. NOTE: Field ip_count has
                            been removed from provider version 1.121.0.'
                          type: number
                        zone:
                          description: '(Removed since v1.121.0) The AZ for the current
                            bandwidth. NOTE: Field zone has been removed from provider
                            version 1.121.0.'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: 'Whether enable the deletion protection or not. Default
                      value: false.'
                    type: boolean
                  description:
                    description: Description of the nat gateway, This description
                      can have a string of 2 to 256 characters, It cannot begin with
                      http:// or https://. Defaults to null.
                    type: string
                  dryRun:
                    description: 'Specifies whether to only precheck this request.
                      Default value: false.'
                    type: boolean
                  eipBindMode:
                    description: 'The EIP binding mode of the NAT gateway. Default
                      value: MULTI_BINDED. Valid values:'
                    type: string
                  force:
                    description: Specifies whether to forcefully delete the NAT gateway.
                    type: boolean
                  icmpReplyEnabled:
                    description: 'Specifies whether to enable ICMP retrieval. Default
                      value: true. Valid values:'
                    type: boolean
                  internetChargeType:
                    description: 'The internet charge type. Valid values PayByLcu.
                      The PayByLcu is only support enhanced NAT. NOTE: From version
                      1.137.0, internet_charge_type cannot be set to PayBySpec.'
                    type: string
                  natGatewayName:
                    description: Name of the nat gateway. The value can have a string
                      of 2 to 128 characters, must contain only alphanumeric characters
                      or hyphens, such as "-",".","_", and must not begin or end with
                      a hyphen, and must not begin with http:// or https://. Defaults
                      to null.
                    type: string
                  natType:
                    description: to Enhanced and set vswitch_id.
                    type: string
                  networkType:
                    description: 'Indicates the type of the created NAT gateway. Valid
                      values internet and intranet. internet: Internet NAT Gateway.
                      intranet: VPC NAT Gateway.'
                    type: string
                  paymentType:
                    description: The billing method of the NAT gateway. Valid values
                      are PayAsYouGo. Default to PayAsYouGo.
                    type: string
                  period:
                    description: |-
                      The duration that you will buy the resource, in month. It is valid when payment_type is Subscription. Valid values: [1-9, 12, 24, 36]. At present, the provider does not support modify "period" and you can do that via web console. NOTE: International station only supports Subscription.
                      -> NOTE: The attribute period is only used to create Subscription instance or modify the PayAsYouGo instance to Subscription.
                    type: number
                  privateLinkEnabled:
                    description: 'Specifies whether to enable PrivateLink. Default
                      value: false. Valid values:'
                    type: boolean
                  spec:
                    description: '(Removed since v1.121.0) The specification of the
                      nat gateway. NOTE: Field spec has been deprecated from provider
                      version 1.7.1, and it has been removed from provider version
                      1.121.0. New field specification instead.'
                    type: string
                  specification:
                    description: The specification of the nat gateway. Valid values
                      are Small, Middle and Large. Effective when internet_charge_type
                      is PayBySpec and network_type is internet. Details refer to
                      Nat Gateway Specification.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The VPC ID.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchId:
                    description: The id of VSwitch.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: NATGatewayStatus defines the observed state of NATGateway.
            properties:
              atProvider:
                properties:
                  accessMode:
                    description: The access mode for reverse access to the VPC NAT
                      gateway. See access_mode below.
                    items:
                      properties:
                        modeValue:
                          description: 'The mode of Access. Valid values:'
                          type: string
                        tunnelType:
                          description: 'The type of Tunnel. Valid values: geneve.
                            NOTE: tunnel_type takes effect only if mode_value is set
                            to tunnel.'
                          type: string
                      type: object
                    type: array
                  bandwidthPackageIds:
                    description: '(Removed since v1.121.0) The ID of the bandwidth
                      package. NOTE: Field bandwidth_package_ids has been removed
                      from provider version 1.121.0.'
                    type: string
                  bandwidthPackages:
                    description: (Removed since v1.121.0) A list of bandwidth packages
                      for the nat gatway. See bandwidth_packages below.
                    items:
                      properties:
                        bandwidth:
                          description: '(Removed since v1.121.0) The bandwidth value
                            of the current bandwidth package. NOTE: Field bandwidth
                            has been removed from provider version 1.121.0.'
                          type: number
                        ipCount:
                          description: '(Removed since v1.121.0) The IP number of
                            the current bandwidth package. NOTE: Field ip_count has
                            been removed from provider version 1.121.0.'
                          type: number
                        publicIpAddresses:
                          description: '(Removed since v1.121.0) The public ip for
                            bandwidth package. NOTE: Field public_ip_addresses has
                            been removed from provider version 1.121.0.'
                          type: string
                        zone:
                          description: '(Removed since v1.121.0) The AZ for the current
                            bandwidth. NOTE: Field zone has been removed from provider
                            version 1.121.0.'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: 'Whether enable the deletion protection or not. Default
                      value: false.'
                    type: boolean
                  description:
                    description: Description of the nat gateway, This description
                      can have a string of 2 to 256 characters, It cannot begin with
                      http:// or https://. Defaults to null.
                    type: string
                  dryRun:
                    description: 'Specifies whether to only precheck this request.
                      Default value: false.'
                    type: boolean
                  eipBindMode:
                    description: 'The EIP binding mode of the NAT gateway. Default
                      value: MULTI_BINDED. Valid values:'
                    type: string
                  force:
                    description: Specifies whether to forcefully delete the NAT gateway.
                    type: boolean
                  forwardTableIds:
                    description: The nat gateway will auto create a forward item.
                    type: string
                  icmpReplyEnabled:
                    description: 'Specifies whether to enable ICMP retrieval. Default
                      value: true. Valid values:'
                    type: boolean
                  id:
                    description: The ID of the nat gateway.
                    type: string
                  internetChargeType:
                    description: 'The internet charge type. Valid values PayByLcu.
                      The PayByLcu is only support enhanced NAT. NOTE: From version
                      1.137.0, internet_charge_type cannot be set to PayBySpec.'
                    type: string
                  natGatewayName:
                    description: Name of the nat gateway. The value can have a string
                      of 2 to 128 characters, must contain only alphanumeric characters
                      or hyphens, such as "-",".","_", and must not begin or end with
                      a hyphen, and must not begin with http:// or https://. Defaults
                      to null.
                    type: string
                  natType:
                    description: to Enhanced and set vswitch_id.
                    type: string
                  networkType:
                    description: 'Indicates the type of the created NAT gateway. Valid
                      values internet and intranet. internet: Internet NAT Gateway.
                      intranet: VPC NAT Gateway.'
                    type: string
                  paymentType:
                    description: The billing method of the NAT gateway. Valid values
                      are PayAsYouGo. Default to PayAsYouGo.
                    type: string
                  period:
                    description: |-
                      The duration that you will buy the resource, in month. It is valid when payment_type is Subscription. Valid values: [1-9, 12, 24, 36]. At present, the provider does not support modify "period" and you can do that via web console. NOTE: International station only supports Subscription.
                      -> NOTE: The attribute period is only used to create Subscription instance or modify the PayAsYouGo instance to Subscription.
                    type: number
                  privateLinkEnabled:
                    description: 'Specifies whether to enable PrivateLink. Default
                      value: false. Valid values:'
                    type: boolean
                  snatTableIds:
                    description: The nat gateway will auto create a snat item.
                    type: string
                  spec:
                    description: '(Removed since v1.121.0) The specification of the
                      nat gateway. NOTE: Field spec has been deprecated from provider
                      version 1.7.1, and it has been removed from provider version
                      1.121.0. New field specification instead.'
                    type: string
                  specification:
                    description: The specification of the nat gateway. Valid values
                      are Small, Middle and Large. Effective when internet_charge_type
                      is PayBySpec and network_type is internet. Details refer to
                      Nat Gateway Specification.
                    type: string
                  status:
                    description: (Available since v1.121.0) The status of NAT gateway.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The VPC ID.
                    type: string
                  vswitchId:
                    description: The id of VSwitch.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
